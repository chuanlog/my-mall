spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/mall_tiny?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username:
    password:
  redis:
    host:  # Redis服务器地址
    database:  # Redis数据库索引（默认为0）
    port:  # Redis服务器连接端口
    password:  # Redis服务器连接密码（默认为空）
    timeout:  # 连接超时时间（毫秒）

logging:
  level:
    root: info
    com.macro.mall: debug

# minio相关配置
minio:
  endpoint:
  accessKey:
  secretKey:
  bucketName: